[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend with Playwright for anime scraping"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]>=0.128.1",
  "playwright>=1.40.0",
  "beautifulsoup4>=4.12.0",
  "lxml>=5.0.0"
]
